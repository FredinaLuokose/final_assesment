<odoo>
    <data>
        <record id="view_move_form_inherit_correction_button" model="ir.ui.view">
            <field name="name">account.move.form.correction.button</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//header" position="inside">
                    <!-- Button labeled "Correction" without any action -->
                    <button name="dummy_action" string="Correction" type="button" class="oe_highlight" />
                </xpath>
            </field>
        </record>
    </data>
</odoo>
